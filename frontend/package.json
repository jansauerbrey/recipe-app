{
  "name": "recipe-app-frontend",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "web",
    "mobile", 
    "shared"
  ],
  "scripts": {
    "web:start": "yarn workspace @recipe-app/web start",
    "web:build": "yarn workspace @recipe-app/web build",
    "mobile:start": "yarn workspace @recipe-app/mobile start",
    "mobile:android": "yarn workspace @recipe-app/mobile android",
    "mobile:ios": "yarn workspace @recipe-app/mobile ios",
    "shared:build": "yarn workspace @recipe-app/shared build",
    "lint": "yarn workspaces run lint",
    "test": "yarn workspaces run test"
  },
  "devDependencies": {
    "typescript": "^4.9.5"
  }
}
